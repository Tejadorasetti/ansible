- name: varibale preference
  hosts: localhost
  connection: local
  vars:
    NAME: "ansible from playbook"
    age: 28
    course: "DevOps"

  vars_files:
    - course.yaml  

  vars_prompt:
  - name: NAME
    prompt: "Please enter your name"
    private: no
    age: "Please enter your age"
    private: yes
    course: "Please enter your course"
    private: no

  tasks:
    - name: "print the message"
      var:
        NAME: "ansible from task"
      ansible.builtin.debug:
        msg: "My name is {{ NAME }}"
