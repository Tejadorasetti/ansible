- name: play level variables 
  hosts: localhost
  connection: local
  tasks:
    - name: "print variables"
      vars:
        course: "AWS"
      ansible.builtin.debug:
        msg: "Course name is {{ course }}."